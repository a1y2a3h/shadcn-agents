# Core dependencies for shadcn-agent
requests>=2.25.0,<3.0.0
langgraph>=0.0.1
deep-translator>=1.9.0,<2.0.0
beautifulsoup4>=4.9.0,<5.0.0
python-dotenv>=0.19.0,<2.0.0

# Optional dependencies for playground (install with: pip install -r requirements.txt[playground])
# streamlit>=1.20.0,<2.0.0

# Development dependencies (uncomment for development)
# pytest>=6.0,<8.0
# pytest-cov>=2.0,<5.0
# black>=21.0,<24.0
# flake8>=3.8,<7.0
# mypy>=0.900,<2.0
# pre-commit>=2.0,<4.0

# Additional useful packages that might be needed
# Pin to a version that provides prebuilt wheels for Python 3.13 on Windows
lxml>=5.2.2,<6.0.0  # Better XML/HTML parsing for BeautifulSoup
urllib3>=1.26.0,<2.0.0  # HTTP library (usually comes with requests)
certifi>=2021.0.0  # SSL certificates (usually comes with requests)

# Email dependencies (usually built-in but might need explicit versions in some environments)
# email-validator>=1.1.0,<3.0.0  # Optional: for email validation